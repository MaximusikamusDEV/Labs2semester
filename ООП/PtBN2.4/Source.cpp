#include "Func.h"

int main()
{
	Complex cmp(3, 1.57);
	Complex cmp2(5, 3.14);
	Complex tempcmp;
	
	
	/*
	cout << cmp2;

	tempcmp = cmp2;
	cmp2 ^ 2;
	cout << cmp2;
	cmp2 = tempcmp;

	tempcmp = cmp;
	cmp + cmp2;
	cmp.OutForPlusAndMinus();
	cmp = tempcmp;

	cmp2 * cmp;
	cout << cmp2;*/

	cmp2 | 3;

	return 0;
}